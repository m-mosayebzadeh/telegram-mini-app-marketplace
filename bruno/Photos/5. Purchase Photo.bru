meta {
  name: 5. Purchase Photo
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/photos/{{photoId}}/purchase
  body: none
  auth: none
}

headers {
  X-Telegram-Init-Data: {{initData}}
}

docs {
  STUB — no real Stars payment yet (see TECHNICAL_REQUIREMENTS.md
  section 7). Grants access unconditionally; only works on a photo with
  is_paid: true (upload one with the paid combo first, see "1. Upload
  Photo" docs).
}
