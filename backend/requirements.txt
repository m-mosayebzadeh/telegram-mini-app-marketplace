# --- web server and core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- reading settings from environment variables (e.g. the bot token) ---
pydantic-settings==2.5.2

# --- ORM / database access ---
sqlalchemy==2.0.52

# --- required by FastAPI to parse multipart/form-data (file uploads) ---
python-multipart==0.0.12

# --- testing ---
pytest==8.3.3
httpx==0.27.2
# Not used by the app itself — only by tests/helpers.py, to generate a
# throwaway JPEG in memory to upload in photo endpoint tests.
pillow==12.3.0
